/** Short explainers for regret, cancel, and delete — shown beside each action. */
export const EVENT_CLOSE_OUT_COPY = {
  regret:
    "Decline this enquiry before it is confirmed. The event moves to Regrets for lost-business review.",
  cancel:
    "Call off a confirmed booking. Requires manager approval; Accounts is notified to review any refund.",
  delete:
    "Remove from calendars and active lists. The record is archived — organisation and contact history are kept.",
} as const;
